# Modo de ejecución (LOCAL | PROD)
# Por defecto se recomienda LOCAL para pruebas de portfolio usando la carpeta data/
EXECUTION_MODE=LOCAL

# Si cambias a PROD, debes completar estas variables:
GOOGLE_APPLICATION_CREDENTIALS="/ruta/absoluta/a/tu/credencial_gcp.json"
GCP_PROJECT_ID="tu-id-de-proyecto-gcp"